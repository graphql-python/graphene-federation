extend schema
	@link(url: "https://specs.apollo.dev/federation/v2.3", import: ["@extends", "@external", "@key"])

type Query {
  a: A
}

type A  @extends {
  id: ID @external
  b(id: ID ): B
}

type B @key(fields: "id") {
  id: ID
}